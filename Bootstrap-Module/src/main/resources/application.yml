spring:
  profiles:
    group:
      dev:
        - security
        - aws
      local:
        - security-local
        - aws-local
      test:
        - security-local
        - aws-local
    active: local